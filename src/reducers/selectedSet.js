/**
 * Created by Jordan3D on 5/4/2018.
 */
// import {
//   SELECT_SET
// } from '../actions'

const initialSet = 'week';

export const selectedSet = (state = initialSet, action) => {
  switch (action.type) {
    // case SELECT_SET :
    //   return setObjectConstruct(action.set);
    default:
      return state
  }
};
